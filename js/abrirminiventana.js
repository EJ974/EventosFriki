function abrirMiniVentanaMoka() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('mokaredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }
  
  function abrirMiniVentanaAincrad() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('aincradredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaNakama() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('nakama_mixredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaElqueBuscaEncuentra() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('elquebuscaencuentraredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaLimonaditas() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('limonaditasredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaLixieStore() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('lixiestoreredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaLetalEnMesa() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('letalmesaredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaFriki3D() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('friki3Dredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaMaviMarroquineria() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('mavimarroquineriaredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaBazaru() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('bazaruredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaParadox() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('paradoxredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaVulcan() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('vulcanredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

  function abrirMiniVentanaMidoriAccesorios() {
    // Abre una nueva ventana con las dimensiones especificadas
    let ventana = window.open('midoriaccesoriosredes.php', 'Mini Ventana', 'width=400,height=400');
    
    // Calcula las coordenadas para centrar la ventana en la pantalla
    let left = (window.screen.width - 400) / 2;
    let top = (window.screen.height - 400) / 2;
    
    // Centra la ventana en la pantalla
    ventana.moveTo(left, top);
  }

